get_filename_component(kimera_dsg_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
include(CMakeFindDependencyMacro)

find_dependency(Boost REQUIRED)

set(PCL_FIND_QUIETLY TRUE)
find_dependency(PCL REQUIRED COMPONENTS)
find_dependency(Eigen3 REQUIRED)

if(NOT TARGET kimera_dsg::kimera_dsg)
    include("${kimera_dsg_CMAKE_DIR}/kimera_dsgTargets.cmake")
endif()

set(kimera_dsg_LIBRARIES kimera_dsg::kimera_dsg)
